/**
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  // Generamos la barra lateral para el CAP manualmente para mantener el orden
  tutorialSidebar: [
    'index',
    'ingreso-sistema',
    'gestion-citas',
    'expedientes',
  ],
};

module.exports = sidebars;